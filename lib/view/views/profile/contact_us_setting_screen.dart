import 'package:flutter/material.dart';

class ContactUsSettingScreen extends StatelessWidget {
  const ContactUsSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}